# Copy this file to `.env.local` and fill in your Google OAuth client secrets.
# `.env.local` is gitignored — real secrets must never be committed.
# (Client IDs are not secret and live in src/cloud.ts.)
VITE_GOOGLE_DESKTOP_SECRET=
VITE_GOOGLE_WEB_SECRET=
